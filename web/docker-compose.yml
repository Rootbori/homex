services:
  web:
    build: .
    container_name: homex-web
    environment:
      - API_BASE_URL=http://host.docker.internal:7772
      - AUTH_URL=http://localhost:7771
      - NODE_ENV=production
    ports:
      - "7771:7771"
    extra_hosts:
      - "host.docker.internal:host-gateway"
